# NestJS API — server-side only, never exposed to the browser
API_URL=http://localhost:4000

# Public site origin — used by metadataBase, sitemap.xml, robots.txt, JSON-LD
NEXT_PUBLIC_SITE_URL=http://localhost:3000
